// const list = document.querySelector('.list');
// const listItem = document.querySelector('.list__item')

// const hello = `<div>Hello World!</div>`
// const helloText = `This is new text "Hello"`

// list.innerHTML = Hello;
// console.log(list.innerHTML)

// listItem.textContent = helloText

//------CREATE NEW ELEMENT-------//
// const newElem = document.createElement('div')
// const textElem = newElem.innerHTML = "It's new element"

// list.before(textElem); //לפני האובייקט
// list.after(textElem); //אחרי האובייקט
// list.prepend(textElem); //התחלה של אובייקט
// list.append(textElem); //סוף האובייקט

// list.before(newElem, 'New elem')//ניתן להוסיף את הטקסט בלי להוסיף אותו בניפרד

/* insert HTML */
// list.insertAdjacentHTML(
//     'afterend',
//     '<p>New elem</p>'
// );

// /* insert ELEMENT */
// list.insertAdjacentText(
//     'beforeend',
//     // `<p>Element for list</p>`
//     `Element for list`
// )

// /* insert ELEMENT */
// const newElement = document.createElement('div');

// newElement.innerHTML = 'New text';

// list.insertAdjacentElement(
//     'beforeend',
//     newElement
// )

// afterbegin, afterend, beforebegin, beforeend
// לבדיקה בעצמכם remove();, cloneNode();

